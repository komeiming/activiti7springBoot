<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.activiti.mapper.TemplateOperationLogMapper">
    
    <resultMap id="BaseResultMap" type="com.itheima.activiti.entity.TemplateOperationLog">
        <id column="id" property="id" />
        <result column="template_id" property="templateId" />
        <result column="template_name" property="templateName" />
        <result column="operation_type" property="operationType" />
        <result column="operator" property="operator" />
        <result column="operation_time" property="operationTime" />
        <result column="ip_address" property="ipAddress" />
        <result column="operation_details" property="operationDetails" />
    </resultMap>
    
    <sql id="Base_Column_List">
        id, template_id, template_name, operation_type, operator, operation_time, ip_address, operation_details
    </sql>
    
    <!-- 按条件查询日志 -->
    <select id="findByCondition" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM template_operation_log
        <where>
            <if test="templateId != null and templateId != ''">
                AND template_id = #{templateId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="operator != null and operator != ''">
                AND operator LIKE CONCAT('%', #{operator}, '%')
            </if>
            <if test="startTime != null">
                AND operation_time >= #{startTime}
            </if>
            <if test="endTime != null">
                <![CDATA[AND operation_time <= #{endTime}]]>
            </if>
        </where>
        ORDER BY operation_time DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>
    
    <!-- 根据模板ID查询操作日志 -->
    <select id="findByTemplateId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM template_operation_log
        WHERE template_id = #{templateId}
        ORDER BY operation_time DESC
    </select>
    
    <!-- 根据操作类型查询日志 -->
    <select id="findByOperationType" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM template_operation_log
        WHERE operation_type = #{operationType}
        ORDER BY operation_time DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>
    
    <!-- 统计日志数量 -->
    <select id="countLogs" resultType="int">
        SELECT COUNT(*)
        FROM template_operation_log
        <where>
            <if test="templateId != null and templateId != ''">
                AND template_id = #{templateId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="startTime != null">
                AND operation_time >= #{startTime}
            </if>
            <if test="endTime != null">
                <![CDATA[AND operation_time <= #{endTime}]]>
            </if>
        </where>
    </select>
    
    <!-- 查询所有日志 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM template_operation_log
        ORDER BY operation_time DESC
    </select>
    
    <!-- 根据ID查询日志 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM template_operation_log
        WHERE id = #{id}
    </select>
    
    <!-- 插入日志 -->
    <insert id="insert">
        INSERT INTO template_operation_log (id, template_id, template_name, operation_type, operator, operation_time, ip_address, operation_details)
        VALUES (#{id}, #{templateId}, #{templateName}, #{operationType}, #{operator}, #{operationTime}, #{ipAddress}, #{operationDetails})
    </insert>
    
    <!-- 按条件查询日志 -->
    <select id="selectByParams" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM template_operation_log
        <where>
            <if test="id != null and id != ''">
                AND id = #{id}
            </if>
            <if test="templateId != null and templateId != ''">
                AND template_id = #{templateId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="operator != null and operator != ''">
                AND operator LIKE CONCAT('%', #{operator}, '%')
            </if>
            <if test="startTime != null">
                AND operation_time >= #{startTime}
            </if>
            <if test="endTime != null">
                <![CDATA[AND operation_time <= #{endTime}]]>
            </if>
        </where>
        ORDER BY operation_time DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>
</mapper>
