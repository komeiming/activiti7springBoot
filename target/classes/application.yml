spring:
  datasource:
    url: jdbc:h2:file:/Users/name01/Documents/GitHub/activiti/activiti7springBoot/data/activiti;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL
    driverClassName: org.h2.Driver
    username: sa
    password: 
    hikari:
      connection-timeout: 10000
      maximum-pool-size: 10
  h2:
      console:
        enabled: true
        path: /h2-console
        settings:
          trace: true
          web-allow-others: true
  activiti:
    database-schema-update: true
    history-level: full
    db-history-used: true
    check-process-definitions: true
    process-definition-location-prefix: classpath*:/processes/
  # 禁用Spring Security的自动配置
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.autoconfigure.security.servlet.SecurityFilterAutoConfiguration
  # 邮件配置
  mail:
    # QQ邮箱SMTP服务器地址
    host: smtp.qq.com
    # QQ邮箱SMTP服务器端口
    port: 465
    # 邮箱用户名（请替换为您的QQ邮箱）
    username: 591462485@qq.com
    # 邮箱密码或授权码（请替换为您的QQ邮箱授权码）
    password: oxptompxzcmcbchh
    # 邮件发送者
    from: 591462485@qq.com
    # 邮件属性配置
    properties:
      mail:
        smtp:
          # 是否需要认证
          auth: true
          # 是否启用STARTTLS
          starttls:
            enable: true
            required: true
          # SSL配置
          ssl:
            enable: true
          # SSL套接字工厂配置
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
    
# MyBatis配置
mybatis:
  # 启用XML映射文件扫描
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.itheima.activiti.entity
  configuration:
    # 设置XML文件编码格式
    default-scripting-language: org.apache.ibatis.scripting.xmltags.XMLLanguageDriver
    default-statement-timeout: 30
    map-underscore-to-camel-case: true

# 服务器配置
server:
  port: 8080
  servlet:
    context-path: /

# 日志配置
logging:
  level:
    com.itheima.activiti: DEBUG
    org.springframework.security: INFO
    org.apache.ibatis: TRACE
    java.sql: DEBUG
